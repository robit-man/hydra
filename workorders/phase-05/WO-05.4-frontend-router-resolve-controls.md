# WO-05.4 Frontend UX Controls For Router Resolve

## Objective
Add explicit router discovery controls to Hydra UI (input, resolve, reconnect, QR, status).

## Files To Touch (Hydra)
- `docs/index.html` header controls near existing transport/health controls (lines 55-60)
- `docs/style.css` transport and control styling section (lines 646-679)
- `docs/js/main.js` UI bindings around `bindUI` (lines 342-364)
- `docs/js/qrScanner.js` scan callback wiring (lines 17-19, 36-54, 99-143)

## Reference Anchors (Teleoperation)
- `/home/robit/Documents/repositories/Dropbear-Neck-Assembly/docs/index.html` router controls and scanner panel (lines 35-56, 59)
- `/home/robit/Documents/repositories/Dropbear-Neck-Assembly/docs/style.css` `.nkn-*` UI styles (lines 505-580)
- `/home/robit/Documents/repositories/Dropbear-Neck-Assembly/docs/script.js` button bindings and status updates (lines 93-96, 2030-2033)

## Implementation Checklist
1. Add router target input + resolve/reconnect/scan buttons.
2. Add status line for resolve health/errors.
3. Bind QR scan to router target parsing.
4. Keep layout responsive for desktop and mobile.

## Success Metrics
- User can configure and resolve router target without dev tools.
- QR scan fills router target input correctly.
- UI displays stale cloudflared warning/error states clearly.

## Potential Failure Modes And Preventions
- Failure mode: controls collide with existing header layout.
  Prevention: responsive CSS breakpoints and compact control group.
- Failure mode: QR scanner overwrites unrelated inputs.
  Prevention: explicit target binding per scan session.
- Failure mode: unclear error messaging.
  Prevention: map backend error codes to user-readable status text.

## Verification
- Manual UI test on desktop and mobile width.
- Scan valid and invalid QR payloads and inspect status feedback.
