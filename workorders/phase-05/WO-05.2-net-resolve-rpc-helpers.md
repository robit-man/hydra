# WO-05.2 Extend Net For Router Resolve + RPC

## Objective
Add NKN helper methods in `Net` for resolve and service RPC without breaking existing flows.

## Files To Touch (Hydra)
- `docs/js/net.js` message handler switch and pending maps (lines 461-537)
- `docs/js/net.js` helper section near `nknSend`, `nknStream`, `relayHealth` (lines 544-712)
- `docs/js/main.js` call sites for new helpers (line 315 onward)

## Reference Anchors (Teleoperation)
- `/home/robit/Documents/repositories/Dropbear-Neck-Assembly/teleoperation/router/router.py` resolve and RPC DM events (lines 2578-2610, 2654-2681)

## Implementation Checklist
1. Add `Net.nknResolveTunnels(targetAddr, timeoutMs)`.
2. Add `Net.nknServiceRpc(service, path, options)`.
3. Add pending maps for `resolve_tunnels_result` and `service_rpc_result`.
4. Keep `relay.health`, `http.request`, and streaming behavior backward compatible.

## Success Metrics
- Existing graph nodes continue operating unchanged.
- New helper methods resolve/reject with deterministic timeout behavior.
- No pending-map leaks after failures.

## Potential Failure Modes And Preventions
- Failure mode: event collision with current pending map keys.
  Prevention: separate namespaces/prefixes for resolve and rpc IDs.
- Failure mode: helper retries duplicate side effects.
  Prevention: no automatic retry for non-idempotent RPC calls.
- Failure mode: parse errors crash message loop.
  Prevention: strict guard clauses and safe JSON handling.

## Verification
- Run existing health check (`relayHealth`) and streaming nodes after patch.
- Exercise both new helper methods with simulated timeout and success.
