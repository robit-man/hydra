# WO-05.3 Endpoint Normalization And Transport Selection

## Objective
Centralize endpoint normalization + transport preference logic in frontend.

## Files To Touch (Hydra)
- `docs/js/endpointResolver.js` (new, line 1)
- `docs/js/main.js` endpoint application path (lines 343-374)
- `docs/js/asr.js` endpoint resolution (lines 95-114)
- `docs/js/tts.js` endpoint resolution (lines 337-356)
- `docs/js/llm.js` endpoint resolution (lines 284-303)
- `docs/js/pointcloud.js` endpoint resolution (lines 91-104)

## Reference Anchors (Teleoperation)
- `/home/robit/Documents/repositories/Dropbear-Neck-Assembly/teleoperation/router/router.py` transport selection in `build_resolved_endpoints` (lines 2249-2265, 2334-2350, 2419-2425)
- `/home/robit/Documents/repositories/Dropbear-Neck-Assembly/docs/script.js` endpoint parsing/normalization helpers (lines 2661-2724)

## Implementation Checklist
1. Normalize variant router payload shapes into one frontend format.
2. Implement deterministic transport preference order (`cloudflare -> upnp -> nats -> nkn -> local`).
3. Reject loopback-only endpoints when router target is remote.
4. Produce diagnostics object (chosen endpoint, reason, rejected candidates).

## Success Metrics
- Endpoint selection is deterministic for identical input snapshot.
- Resolver rejects non-routable loopback endpoints in remote mode.
- Node modules consume resolver output without duplicated heuristics.

## Potential Failure Modes And Preventions
- Failure mode: inconsistent selection between nodes.
  Prevention: all nodes consume single resolver module.
- Failure mode: stale endpoint overwrite.
  Prevention: compare timestamp/request ID before apply.
- Failure mode: missing fallback candidate causes null endpoint.
  Prevention: explicit fallback walk with last-resort local mode.

## Verification
- Feed snapshot fixtures and assert chosen transport/base URL outputs.
- Switch remote/local modes and confirm loopback filtering behavior.
