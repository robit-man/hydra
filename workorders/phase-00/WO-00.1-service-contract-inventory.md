# WO-00.1 Service Contract Inventory

## Objective
Create a canonical, machine-readable contract matrix for Hydra services and teleoperation reference services.

## Files To Touch (Hydra)
- `docs/HYDRA_SERVICE_CONTRACT_MATRIX.json` (new, start at line 1)
- `docs/HYDRA_SERVICE_CONTRACT_MATRIX.md` (new, start at line 1)
- `service_router/router.py` around `class ServiceWatchdog` (line 319), `_default_config` (line 1122), `load_config` (line 1169), `class Router` (line 5339)
- `service_router/router_config.json` targets and assignments (lines 3-10, 29-94)

## Reference Anchors (Teleoperation)
- `/home/robit/Documents/repositories/Dropbear-Neck-Assembly/teleoperation/router/router.py` routes and payload shape (lines 3395-3502, 3526-3652)
- `/home/robit/Documents/repositories/Dropbear-Neck-Assembly/teleoperation/adapter/adapter.py` `health`/`router_info` and fallback payloads (lines 2470, 2658-2719, 2753)
- `/home/robit/Documents/repositories/Dropbear-Neck-Assembly/teleoperation/vision/camera_route.py` `tunnel_info` and `router_info` (lines 4989-5033, 5040-5104)
- `/home/robit/Documents/repositories/Dropbear-Neck-Assembly/teleoperation/audio/audio_route.py` `tunnel_info` and `router_info` (lines 2225-2269, 2276-2339)

## Implementation Checklist
1. Inventory every Hydra service endpoint currently used by frontend and router relay.
2. Record request/response schema for `/health`, model list, stream, and command endpoints.
3. Define canonical Hydra schemas for `health`, `router_info`, `tunnel_info` (JSON Schema sections).
4. Add a compatibility table mapping old endpoint shapes to canonical fields.

## Success Metrics
- Contract JSON exists and validates with `jq` (`jq . docs/HYDRA_SERVICE_CONTRACT_MATRIX.json`).
- Matrix includes every service from `router_config.json` (`ollama`, `asr`, `tts`, `mcp`, `web_scrape`, `depth_any`).
- Canonical schema declares required fields and optional fallback blocks.

## Potential Failure Modes And Preventions
- Failure mode: Missing one service in matrix.
  Prevention: Cross-check service IDs against `router_config.json` lines 3-10 and 87-94.
- Failure mode: Schema too strict for legacy payloads.
  Prevention: Include compatibility coercion notes per field.
- Failure mode: Drift between code and matrix.
  Prevention: Add update instructions and ownership in markdown header.

## Verification
- `jq '.services | keys' docs/HYDRA_SERVICE_CONTRACT_MATRIX.json`
- `rg "depth_any|web_scrape|mcp" docs/HYDRA_SERVICE_CONTRACT_MATRIX.*`
