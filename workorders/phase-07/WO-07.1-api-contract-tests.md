# WO-07.1 API Contract Tests

## Objective
Add automated tests for control-plane and resolver payload contracts.

## Files To Touch (Hydra)
- `service_router/tests/test_api_contracts.py` (new, line 1)
- `service_router/tests/test_resolver_normalization.py` (new, line 1)
- `service_router/router.py` route payload builders under `class Router` (line 5339)
- `docs/HYDRA_SERVICE_CONTRACT_MATRIX.json` source schema (line 1)

## Reference Anchors (Teleoperation)
- `/home/robit/Documents/repositories/Dropbear-Neck-Assembly/teleoperation/router/router.py` payload expectations for `/health`, `/services/snapshot`, `/nkn/info`, `/nkn/resolve` (lines 3447-3652)

## Implementation Checklist
1. Add schema tests for `GET /health`, `GET /services/snapshot`, `GET /nkn/info`, `POST /nkn/resolve`.
2. Add normalization tests for variant `health/router_info/tunnel_info` source shapes.
3. Add regression fixtures for stale tunnel and fallback selection cases.
4. Wire tests into CI entry command.

## Success Metrics
- Tests fail on missing required contract fields.
- Resolver normalization fixtures cover at least 8 edge cases.
- CI command exits non-zero on schema regressions.

## Potential Failure Modes And Preventions
- Failure mode: brittle tests against non-deterministic fields.
  Prevention: ignore timestamps/random IDs via schema rules.
- Failure mode: low fixture coverage.
  Prevention: include malformed, partial, and stale endpoint fixtures.
- Failure mode: tests require live network.
  Prevention: use mocked snapshot payloads and local test client.

## Verification
- `pytest service_router/tests/test_api_contracts.py -q`
- `pytest service_router/tests/test_resolver_normalization.py -q`
